package pageobjects.components;

import org.openqa.selenium.WebDriver;

public class HomePageNavBar extends AbstractNavBar {

    public HomePageNavBar(WebDriver driver) {
        super(driver);
    }
}
